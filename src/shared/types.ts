export enum SelectedPage {
    Home = "home",
    About = "about",
    Services = "services",
    Contact = "contact"
}

export interface heroslideType {
    heroImage: string;
}

export interface serviceType {
    image: string;
    title: string;
    description: string;
}